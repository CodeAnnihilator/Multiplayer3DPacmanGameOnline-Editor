import React, { Component } from 'react'

import styles from './rightSideBarStyles.module.scss'

export default class RightSideBar extends Component {
  render() {
    return (
      <div className={styles.wrapper}>RIGHT SIDEBAR</div>
    )
  }
}
